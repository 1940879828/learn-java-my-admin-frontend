export enum DataScope {
  ALL = 'ALL',
  CUSTOM = 'CUSTOM',
  DEPT = 'DEPT',
  DEPT_AND_CHILD = 'DEPT_AND_CHILD',
  SELF = 'SELF',
}

export enum MenuType {
  DIR = 'DIR',
  MENU = 'MENU',
  BUTTON = 'BUTTON',
}
